version: "2.2"
services:
  myblazorserverapp:
    build: ../../blazorwasm

  reverse_proxy:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 8080:80
    restart: unless-stopped